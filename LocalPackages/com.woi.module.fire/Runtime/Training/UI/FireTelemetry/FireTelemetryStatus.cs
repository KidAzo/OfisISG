namespace Woi.Game.Training.UI.FireTelemetry
{
    /// <summary>Semantic styling for telemetry rows (aim, distance bar accent, etc.).</summary>
    public enum FireTelemetryStatus
    {
        Neutral,
        Ok,
        Warning,
        Danger,
        Wrong
    }
}
